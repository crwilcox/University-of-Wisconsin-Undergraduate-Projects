To Build:
All you should need to do to build is run make with the arm compiler.

To Run:
Then on first run do ./pong -c
This is for configuration.  Follow the prompts.  This will fill out a configuration file to make the photoresistors function

For a regular run just use ./pong

To specify a port other than 8332, use ./pong -p xxxx